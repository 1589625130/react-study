import { FC, Fragment, ReactElement } from 'react'
import { Form } from 'antd'

const AddComment: FC = (): ReactElement => {
  const [form] = Form.useForm()

  return <Fragment></Fragment>
}
export default AddComment
